stages:
  - name: Build image
    steps:
      - publishImageConfig:
          dockerfilePath: ./Dockerfile
          buildContext: .
          tag: harshadadeokar/sample-api
          pushRemote: true
          

  - name: Deploy
    steps:
      - applyYamlConfig:
          path: ./kubernetes.yaml
